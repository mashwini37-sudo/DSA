class Solution:
    def isArmstrong(self, n):
        if n < 0:
            return False

        original = n
        num_digit = len(str(n))
        total = 0

        while n != 0:
            digit = n % 10
            total +=  digit ** num_digit
            n = n//10
        return original == total     