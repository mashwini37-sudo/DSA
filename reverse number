class Solution:
    def reverse(self, n: int) -> int:
        sign = -1 if n < 0 else 1
        n = abs(n)
        
        reversed_n = 0
        while n > 0:
            digit = n % 10
            reversed_n = reversed_n * 10 + digit
            n //= 10
        
        return sign * reversed_n